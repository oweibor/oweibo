# Mirror .gitignore: secretlint scans **/* and would otherwise flag content
# in directories git already ignores (e.g. third-party reference projects
# placed under plans/).
plans/**
node_modules/**
*.tsbuildinfo
dist/**
**/dist/**
.next/**
**/.next/**
config.env*
