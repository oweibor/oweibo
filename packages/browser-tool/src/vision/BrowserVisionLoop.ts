// packages/browser-tool/src/vision/BrowserVisionLoop.ts
// Backwards-compatibility re-export shim (v9.5.9).
// Canonical class is BrowserVisionBridge — import from there for new code.
export { BrowserVisionBridge as BrowserVisionLoop } from './BrowserVisionBridge.js';
