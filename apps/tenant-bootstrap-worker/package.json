{
  "name": "@oweibo/tenant-bootstrap-worker",
  "version": "0.1.0",
  "private": true,
  "description": "T.1 — Consumes tenant.created.v1 lifecycle events and runs the per-tenant bootstrap step pipeline.",
  "main": "./dist/index.js",
  "scripts": {
    "build":      "tsc --project tsconfig.json",
    "type-check": "tsc --noEmit",
    "start":      "node dist/index.js",
    "test":       "jest --passWithNoTests",
    "clean":      "rimraf dist"
  },
  "dependencies": {
    "@oweibo/core-contracts": "workspace:*",
    "@oweibo/core-engine": "workspace:*",
    "ioredis": "^5.4.1",
    "pg": "^8.13.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/pg": "^8.11.10",
    "@types/jest": "^29.5.13",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "rimraf": "^5.0.0",
    "typescript": "^5.5.4"
  }
}
