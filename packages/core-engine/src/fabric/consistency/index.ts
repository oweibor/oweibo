/**
 * ADR-003 consistency contracts (K.3 substrate): the §14.3 comparison
 * rule, monotonic vector merge, conflict classification, chunk-diff,
 * pending edges, and the published §15/§16.1 tables.
 */
export * from './contract.js';
